# 🎨 Guía Rápida de Íconos - CETAM v4.0

## Referencia Rápida para Desarrolladores

**Uso:** `AppIcon(name: AppIconName.NOMBRE)`

---

## 👤 USUARIOS Y PERFILES

| Alias | Uso | Código |
|-------|-----|--------|
| `user` | Usuario genérico | `AppIcon(name: AppIconName.user)` |
| `userCircle` | Perfil destacado | `AppIcon(name: AppIconName.userCircle)` |
| `userAdd` | Agregar usuario | `AppIcon(name: AppIconName.userAdd)` |
| `userRemove` | Quitar usuario | `AppIcon(name: AppIconName.userRemove)` |
| `userGroup` | Grupo de usuarios | `AppIcon(name: AppIconName.userGroup)` |
| `userTie` | Usuario administrativo | `AppIcon(name: AppIconName.userTie)` |

---

## ✏️ ACCIONES CRUD

| Alias | Uso | Código |
|-------|-----|--------|
| `add` o `create` | Crear nuevo | `AppIcon(name: AppIconName.add)` |
| `edit` | Editar | `AppIcon(name: AppIconName.edit)` |
| `delete` | Eliminar | `AppIcon(name: AppIconName.delete)` |
| `view` | Ver detalles | `AppIcon(name: AppIconName.view)` |
| `save` | Guardar | `AppIcon(name: AppIconName.save)` |
| `cancel` | Cancelar | `AppIcon(name: AppIconName.cancel)` |
| `send` | Enviar | `AppIcon(name: AppIconName.send)` |
| `download` | Descargar | `AppIcon(name: AppIconName.download)` |
| `upload` | Subir archivo | `AppIcon(name: AppIconName.upload)` |
| `search` | Buscar | `AppIcon(name: AppIconName.search)` |
| `refresh` | Actualizar | `AppIcon(name: AppIconName.refresh)` |

---

## 🔔 ESTADOS Y NOTIFICACIONES

| Alias | Uso | Color Sugerido | Código |
|-------|-----|----------------|--------|
| `success` | Éxito | `Color(0xFF10B981)` | `AppIcon(name: AppIconName.success, color: Color(0xFF10B981))` |
| `error` | Error | `Color(0xFFE11D48)` | `AppIcon(name: AppIconName.error, color: Color(0xFFE11D48))` |
| `warning` | Advertencia | `Color(0xFFFBA918)` | `AppIcon(name: AppIconName.warning, color: Color(0xFFFBA918))` |
| `info` | Información | `Color(0xFF1E90FF)` | `AppIcon(name: AppIconName.info, color: Color(0xFF1E90FF))` |
| `pending` | Pendiente | `Color(0xFFFBA918)` | `AppIcon(name: AppIconName.pending, color: Color(0xFFFBA918))` |
| `notification` | Notificación | - | `AppIcon(name: AppIconName.notification)` |
| `notificationOff` | Silenciado | `Color(0xFF6B7280)` | `AppIcon(name: AppIconName.notificationOff, color: Color(0xFF6B7280))` |

---

## 📁 ARCHIVOS Y DOCUMENTOS

| Alias | Uso | Código |
|-------|-----|--------|
| `file` | Archivo genérico | `AppIcon(name: AppIconName.file)` |
| `filePdf` | PDF | `AppIcon(name: AppIconName.filePdf)` |
| `fileWord` | Word | `AppIcon(name: AppIconName.fileWord)` |
| `fileExcel` | Excel | `AppIcon(name: AppIconName.fileExcel)` |
| `fileImage` | Imagen | `AppIcon(name: AppIconName.fileImage)` |
| `folder` | Carpeta cerrada | `AppIcon(name: AppIconName.folder)` |
| `folderOpen` | Carpeta abierta | `AppIcon(name: AppIconName.folderOpen)` |
| `attachment` | Adjunto | `AppIcon(name: AppIconName.attachment)` |

---

## 🏠 NAVEGACIÓN

| Alias | Uso | Código |
|-------|-----|--------|
| `home` | Inicio | `AppIcon(name: AppIconName.home)` |
| `dashboard` | Dashboard | `AppIcon(name: AppIconName.dashboard)` |
| `menu` | Menú hamburguesa | `AppIcon(name: AppIconName.menu)` |
| `back` | Regresar | `AppIcon(name: AppIconName.back)` |
| `forward` | Adelante | `AppIcon(name: AppIconName.forward)` |
| `up` | Arriba | `AppIcon(name: AppIconName.up)` |
| `down` | Abajo | `AppIcon(name: AppIconName.down)` |
| `close` | Cerrar | `AppIcon(name: AppIconName.close)` |
| `externalLink` | Enlace externo | `AppIcon(name: AppIconName.externalLink)` |

---

## ⚙️ CONFIGURACIÓN Y PROCESOS

| Alias | Uso | Código |
|-------|-----|--------|
| `settings` | Configuración | `AppIcon(name: AppIconName.settings)` |
| `loading` | Cargando | `AppIcon(name: AppIconName.loading)` |
| `process` | Proceso | `AppIcon(name: AppIconName.process)` |
| `sync` | Sincronizar | `AppIcon(name: AppIconName.sync)` |

---

## 💰 FINANZAS

| Alias | Uso | Código |
|-------|-----|--------|
| `money` | Dinero | `AppIcon(name: AppIconName.money)` |
| `coins` | Monedas | `AppIcon(name: AppIconName.coins)` |
| `card` | Tarjeta | `AppIcon(name: AppIconName.card)` |
| `invoice` | Factura | `AppIcon(name: AppIconName.invoice)` |
| `chartUp` | Gráfico ascendente | `AppIcon(name: AppIconName.chartUp)` |
| `chartDown` | Gráfico barras | `AppIcon(name: AppIconName.chartDown)` |

---

## 📧 COMUNICACIÓN

| Alias | Uso | Código |
|-------|-----|--------|
| `email` | Email | `AppIcon(name: AppIconName.email)` |
| `phone` | Teléfono | `AppIcon(name: AppIconName.phone)` |
| `chat` | Chat | `AppIcon(name: AppIconName.chat)` |
| `support` | Soporte | `AppIcon(name: AppIconName.support)` |
| `help` | Ayuda | `AppIcon(name: AppIconName.help)` |

---

## 📋 LISTAS Y FILTROS

| Alias | Uso | Código |
|-------|-----|--------|
| `list` | Lista | `AppIcon(name: AppIconName.list)` |
| `listOrdered` | Lista numerada | `AppIcon(name: AppIconName.listOrdered)` |
| `filter` | Filtrar | `AppIcon(name: AppIconName.filter)` |
| `sort` | Ordenar | `AppIcon(name: AppIconName.sort)` |
| `sortUp` | Ascendente | `AppIcon(name: AppIconName.sortUp)` |
| `sortDown` | Descendente | `AppIcon(name: AppIconName.sortDown)` |
| `checkList` | Lista verificación | `AppIcon(name: AppIconName.checkList)` |

---

## 🔐 SEGURIDAD

| Alias | Uso | Color Sugerido | Código |
|-------|-----|----------------|--------|
| `login` | Iniciar sesión | `Color(0xFF10B981)` | `AppIcon(name: AppIconName.login, color: Color(0xFF10B981))` |
| `logout` | Cerrar sesión | `Color(0xFFE11D48)` | `AppIcon(name: AppIconName.logout, color: Color(0xFFE11D48))` |
| `lock` | Bloqueado | - | `AppIcon(name: AppIconName.lock)` |
| `unlock` | Desbloqueado | - | `AppIcon(name: AppIconName.unlock)` |
| `key` | Llave/Credenciales | - | `AppIcon(name: AppIconName.key)` |
| `shield` | Seguridad | - | `AppIcon(name: AppIconName.shield)` |

---

## 📊 REPORTES

| Alias | Uso | Código |
|-------|-----|--------|
| `report` | Reporte línea | `AppIcon(name: AppIconName.report)` |
| `reportBar` | Reporte barras | `AppIcon(name: AppIconName.reportBar)` |
| `reportPie` | Reporte pastel | `AppIcon(name: AppIconName.reportPie)` |
| `print` | Imprimir | `AppIcon(name: AppIconName.print)` |
| `downloadReport` | Descargar reporte | `AppIcon(name: AppIconName.downloadReport)` |

---

## 🎨 EJEMPLOS DE USO COMÚN

### En Botones:
```dart
ElevatedButton.icon(
  icon: AppIcon(name: AppIconName.save, size: 20, color: Colors.white),
  label: Text('Guardar'),
  onPressed: () {},
)
```

### En ListTile:
```dart
ListTile(
  leading: AppIcon(name: AppIconName.user),
  title: Text('Mi perfil'),
  trailing: AppIcon(name: AppIconName.forward, size: 20),
  onTap: () {},
)
```

### En TextField:
```dart
TextField(
  decoration: InputDecoration(
    prefixIcon: AppIcon(
      name: AppIconName.email,
      color: Color(0xFFADB5BD),
      size: 20,
    ),
    hintText: 'Correo electrónico',
  ),
)
```

### En AlertDialog:
```dart
AlertDialog(
  icon: AppIcon(
    name: AppIconName.warning,
    color: Color(0xFFFBA918),
    size: 32,
  ),
  title: Text('Advertencia'),
  content: Text('Esta acción requiere confirmación'),
)
```

### Con Color Dinámico:
```dart
AppIcon(
  name: status == 'active' ? AppIconName.success : AppIconName.error,
  color: status == 'active' ? Color(0xFF10B981) : Color(0xFFE11D48),
)
```

---

## ⚡ ATAJOS DE TECLADO (VS Code)

Para reemplazar rápidamente:

1. `Ctrl+H` (Buscar y Reemplazar)
2. Buscar: `Icon\(Icons\.(\w+)`
3. Reemplazar: `AppIcon(name: AppIconName.$1`
4. ⚠️ **REVISAR CADA CASO** - Los nombres pueden variar

---

## 📝 REGLAS IMPORTANTES

1. **Tamaño por defecto:** 24px (no cambiar sin justificación)
2. **Color:** Usar `color` parameter o dejar que use el tema
3. **Nunca:** Usar `Icon(Icons.` directamente
4. **Siempre:** Verificar que el alias existe en `app_icons.dart`

---

## 🔍 BÚSQUEDA RÁPIDA

¿No encuentras el ícono? Busca por concepto:

- **Usuario** → `user`, `userCircle`, `userGroup`
- **Editar** → `edit`
- **Borrar** → `delete`
- **Guardar** → `save`
- **Buscar** → `search`
- **Config** → `settings`
- **Notif** → `notification`
- **Archivo** → `file`, `filePdf`, `fileWord`
- **Login** → `login`, `logout`
- **Casa** → `home`
- **Menú** → `menu`

---

**Versión:** 4.0  
**Última actualización:** 27 de noviembre de 2025  
**Total de íconos:** 60+
